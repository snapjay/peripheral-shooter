'use strict';

describe('myApp.shooter module', function() {

  beforeEach(module('myApp.shooter'));

  describe('shooter controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});